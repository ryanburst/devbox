# Copy to env.local and uncomment as needed for corporate networks.
# install.sh sources config/env.local when present.

# export HTTP_PROXY=http://proxy.company.com:8080
# export HTTPS_PROXY=http://proxy.company.com:8080
# export NO_PROXY=localhost,127.0.0.1,.company.com

# Node tooling behind TLS inspection (use only if installs fail on certs)
# export DEVBOX_NPM_STRICT_SSL=false

# Override defaults
# export DEVBOX_NODE_VERSION=22
# export DEVBOX_PNPM_STORE=$HOME/.pnpm-store
# export DEVBOX_CODE_DIR=$HOME/code
